package com.example.studentcrud.Service;

import com.example.studentcrud.Entity.RefreshToken;
import com.example.studentcrud.Repository.RefreshTokenRepository;
import org.junit.jupiter.api.Test;
import org.mapstruct.factory.Mappers;

import static org.junit.jupiter.api.Assertions.*;

class StudentMapperTest {
    // MapStruct generated implementation is available at compile time; use Mappers when not running Spring context
    @Test
    void mappingStudentToResponse() {
        // This is a lightweight test placeholder — for full tests use Spring Boot test with sample entities
        assertTrue(true);
    }
}
